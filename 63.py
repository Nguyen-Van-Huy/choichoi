>>> list(enumerate([2, 3]))
[(0, 2), (1, 3)]
#Hàm enumerate() thêm vào một bộ đếm vào trước mỗi iterable
#và trả về kết quả dưới dạng đối tượng liệt kê. Các đối tượng enumerate này sau đó có thể được sử dụng trực tiếp trong các vòng lặp hoặc được chuyển đổi thành một danh sách,
#một tuple bằng phương thức list() và tuple().
